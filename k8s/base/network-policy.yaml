# security-aware ingress rules restricting traffic flow exclusively to authorized Traefik routes
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: api-allow-traefik
  namespace: foobar-namespace
spec:
  podSelector:
    matchLabels:
      app: foobar-api
  policyTypes:
  - Ingress
  ingress:
  - from:
    - podSelector:
        matchLabels:
          app.kubernetes.io/name: traefik